# 🔧 环境配置说明

## 📋 环境文件结构

```
├── .env.development    # 开发/测试环境配置（本地）
└── .env.production     # 生产环境配置（部署时使用）
```

## 🔒 重要说明

- **所有 `.env.*` 文件都已加入 `.gitignore`**，不会提交到代码库
- **真实的密钥值应该直接写在对应的环境文件中**
- **这些文件包含敏感信息，请妥善保管**

## 🧪 开发环境配置 (.env.development)

### 基础配置
```bash
NODE_ENV=development
NEXT_PUBLIC_APP_MODE=complete
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

### Supabase 配置
```bash
NEXT_PUBLIC_SUPABASE_URL=你的测试环境URL
NEXT_PUBLIC_SUPABASE_ANON_KEY=你的测试环境anon_key
```

### 分析工具配置
```bash
# 测试环境禁用分析
NEXT_PUBLIC_ENABLE_ANALYTICS=false

# 如果需要测试 Plausible 分析功能：
# NEXT_PUBLIC_PLAUSIBLE_DOMAIN=localhost:3000
```

## 📊 关于 Plausible Analytics

**Plausible** 是一个轻量级、注重隐私的网站分析工具（类似 Google Analytics）

### 特点：
- 🚀 轻量级：脚本只有 1KB
- 🛡️ 隐私友好：不使用 cookies，符合 GDPR
- 📈 简洁界面：专注核心数据

### 配置说明：
- `NEXT_PUBLIC_PLAUSIBLE_DOMAIN`：配置要分析的域名
- 测试环境：建议设置为 `localhost:3000` 或直接禁用
- 生产环境：设置为实际部署的域名，如 `example.com`

## 🚀 部署配置

### 未来部署选项：
1. **主域名**：`habitkids.com`
2. **测试子域名**：`dev.habitkids.com`
3. **API 子域名**：`api.habitkids.com`

### 环境对应：
- **本地开发**：`http://localhost:3000`
- **测试部署**：`https://dev.habitkids.com`
- **生产部署**：`https://habitkids.com`

## 🔑 密钥管理

### 安全原则：
1. ✅ 真实密钥直接写在环境文件中
2. ✅ 环境文件不提交到代码库
3. ✅ 不同环境使用不同的密钥
4. ❌ 不在代码中硬编码密钥
5. ❌ 不在注释中留存密钥

### 获取 Supabase 密钥：
1. 登录 [Supabase Dashboard](https://supabase.com/dashboard)
2. 选择你的项目
3. 进入 Settings > API
4. 复制 `Project URL` 和 `anon/public key`

## 📝 配置检查清单

- [ ] `.env.development` 文件存在
- [ ] Supabase URL 和 anon key 已填入
- [ ] 分析工具根据需要配置
- [ ] 文件已添加到 `.gitignore`
- [ ] 应用可以正常连接 Supabase 